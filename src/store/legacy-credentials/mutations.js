export const updateTicket = (state, ticket) => {
    state.ticket = ticket;
}

export const updateCSRFPreventionToken = (state, CSRFPreventionToken) => {
    state.CSRFPreventionToken = CSRFPreventionToken;
}

export const updateCap = (state, cap) => {
    state.cap = cap;
}

export const updateUsername = (state, username) => {
    state.username = username;
}
